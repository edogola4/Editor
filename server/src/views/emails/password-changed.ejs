<%- include('base', { 
  subject: 'Your Password Has Been Changed',
  appName: typeof appName !== 'undefined' ? appName : 'Collaborative Editor',
  appUrl: typeof appUrl !== 'undefined' ? appUrl : 'http://localhost:3000',
  supportEmail: typeof supportEmail !== 'undefined' ? supportEmail : 'support@collabeditor.com',
  to: typeof to !== 'undefined' ? to : ''
}) %>

<h1>Password Successfully Changed</h1>

<p>Hello <%= name %>,</p>

<p>This is a confirmation that the password for your <%= appName %> account has been changed on <strong><%= new Date(timestamp).toLocaleString() %></strong>.</p>

<div style="background-color: #f3f4f6; padding: 15px; border-radius: 6px; margin: 20px 0;">
  <p style="margin: 0;">
    <strong>Important:</strong> If you did not make this change, please contact our support team immediately at 
    <a href="mailto:<%= supportEmail || 'support@collabeditor.com' %>"><%= supportEmail || 'support@collabeditor.com' %></a>.
  </p>
</div>

<p>For security reasons, this is an automated message. Please do not reply to this email.</p>

<p>Best regards,<br>The <%= appName %> Security Team</p>

<div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #e5e7eb; font-size: 14px; color: #6b7280;">
  <p>This email was sent from a notification-only address that cannot accept incoming email. 
  Please do not reply to this message.</p>
  
  <p>
    <a href="<%= appUrl %>/account/security">Manage your account security settings</a> | 
    <a href="<%= appUrl %>/help/security">Learn more about account security</a>
  </p>
</div>
